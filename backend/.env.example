# Server
PORT=4000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:5173

# Supabase (Service role required)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Email (Gmail SMTP)
# Set EMAIL_ENABLED=false to disable emails locally
EMAIL_ENABLED=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
# Use your Gmail address and an App Password (not your regular password)
SMTP_USER=
SMTP_PASS=your-gmail-app-password
# From and Reply-To; defaults to SMTP_USER if not set
SMTP_FROM=awscloudclubfrizz@gmail.com
SMTP_REPLY_TO=johnlexterreyesss@gmail.com

